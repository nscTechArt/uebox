# 安全策略 / Security Policy

## 报告漏洞

**请不要用公开 issue 报告安全漏洞。**

请发送到 **[admin@uebox.ai](mailto:admin@uebox.ai)**，由维护者私下处理。
这个渠道不依赖 GitHub 或 Gitee 的仓库功能是否启用。

报告里请包含：

- 受影响的版本与操作系统
- 复现步骤（能给最小复现最好）
- 你评估的影响面

我们会在 **3 个工作日**内确认收到，并在评估后告知修复计划。修复发布时，
如果你愿意，我们会在发布说明里致谢。

## 支持范围

只有最新发行版会收到安全修复。

## 这个应用会碰什么

报漏洞前可以先看这里，判断是不是预期行为：

| 数据               | 存放位置                                       | 说明                                                                       |
| ------------------ | ---------------------------------------------- | -------------------------------------------------------------------------- |
| AI Provider 密钥   | `%APPDATA%/unreal-box/ai-provider-secrets.bin` | 经 Electron `safeStorage` 加密。安全存储不可用时**拒绝落盘**，不降级成明文 |
| Provider 配置      | `%APPDATA%/unreal-box/models.json`             | 明文，但**不含密钥** —— 只存密钥引用（id、环境变量名、或取密钥的命令）     |
| 自建资产平台会话 | `%APPDATA%/unreal-box/asset-platform/session.enc` | 经 `safeStorage` 加密；不可用时拒绝保存。旧版明文会话会迁移，无法加密则清除并要求重新登录 |
| 资产 / 笔记 / 蓝图 | 用户选择的本地目录及 SQLite 索引                                    | 不加密                                                                     |
| 本机身份           | 本地                                           | 随机生成，不含任何凭据                                                     |

社区版**不需要账号，也不与官方服务器通信**。仓库里有一条门禁
（`scripts/verify-offline-boot.mjs`）在打包产物上逐页验证这一点。

## 已知的设计取舍

以下是有意为之，不算漏洞：

- **`!命令` 形式的密钥来源会执行 shell 命令。** 命令来自本机的 `models.json`
  或用户在设置里手输，信任级别与 `.bashrc`、git 的 credential helper 相同 ——
  能写到那个文件的人本来就能在这台机器上执行任意命令。
  但如果你能构造出**远端内容**（模型输出、网页、导入的配置）流进这条路径，
  那是真漏洞，请报告。

- **本地插件 HTTP 服务器**监听回环地址，供虚幻引擎插件调用。
  若你发现它可被外部网络访问，或存在越权读写本机文件的路径，请报告。

## 崩溃上报

社区版没有向官方服务器发送崩溃报告或遥测的通道。

日志平时写入本机文件（`electron-log`）。但连接了**用户自行配置的资产服务器 / NAS** 后，
远程导入失败可能自动上传诊断包到该服务器，重试队列也会补传。
诊断包可能包含设备与版本信息、导入报告、错误详情及截取的日志。
打包时会替换用户主目录和导入根目录等路径；这不保证日志中的所有敏感内容都已被移除。
连接资产服务器前，应确认你信任它的运营者。这个通道见
[`DiagnosticUploadService.ts`](src/main/services/system/DiagnosticUploadService.ts)。

若你要为自己的分发版加崩溃上报，请通过 `@edition` 接缝在自己的版本实现里做，
**不要把 DSN 写进核心源码** —— 那会让所有 fork 出去的构建都把崩溃发回同一个项目。

## 依赖安全

CI 会跑 `pnpm audit:prod`。第三方组件的许可证与分发说明见
[THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md)。
