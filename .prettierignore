out
dist
pnpm-lock.yaml
LICENSE.md
tsconfig.json
tsconfig.*.json

# 第三方 vendored bundle（draco / ueblueprint），不是我们的代码
src/renderer/public

# 本地草稿、运行时上传目录
.test
.doc
uploads

# 文档站的构建产物与本地缓存（正文的 .md 仍然由 prettier 管）
website/.vitepress/dist
website/.vitepress/cache
