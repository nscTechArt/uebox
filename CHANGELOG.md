# 更新日志

本文件记录社区版的用户可见变化。格式参考
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[语义化版本](https://semver.org/lang/zh-CN/)。

「破坏性」条目指升级后行为会变、或需要用户重新配置的改动。

## [1.0.3] - 2026-09-24

### 新增

- 创作者 Token Plan（设置 → 模型）：设备授权连一次，对话、嵌入、生图、视频 / 3D / 音乐、
  语音合成与识别、实时语音、网页检索、判定一起配好，对象存储也可改用套餐自带的存储，不用 AK/SK。
  导入前先预览要接管哪些角色；断开时吊销 Key，还原接管前的绑定。卡片上只显示剩余额度百分比，
  细账在网页端。音乐标 Beta。没连接时不发任何请求
- 服务商目录加「订阅套餐」一组：小米 MiMo Token Plan、阿里云百炼 Token / Coding Plan、
  智谱 GLM Coding Plan、火山方舟 Coding Plan、腾讯云 Coding / Token Plan、MiniMax Token Plan、
  阶跃星辰 Step Plan。套餐 Key 与按量账户不通用，各自单列。另加按量的小米 MiMo，
  以及 GPT-6 Sol / Luna、Claude Opus 5.5
- 生视频支持参考视频和参考音频（方舟 Seedance 2.x）。本地视频经对象存储换成链接，
  没配对象存储时在扣费之前拦下
- 语音下指令：热键唤起 Spotlight 只转写不回答，说完填进输入框、倒计时自动提交；
  按住热键说话、松手即发。默认关闭（Alt+Q），在偏好设置 → 快捷键 里打开「语音下指令」
- 语音识别成为独立角色，接入豆包 STT 2.0 与 Qwen-Audio ASR。此前听写借实时语音通道跑，
  绑豆包的用户按下热键只会得到一句「这会儿用不了语音」
- 播报风格三档：简洁 / 详细 / 完整。前两档朗读前先由轻量任务模型压成口播稿，只念结论或要点；
  完整档原文照念，也是默认档。压缩超时、模型没配、回空稿一律退回念原文
- 小窗回复落定后自动朗读，气泡上加朗读按钮
- 回声门限做成可配（耳机 / 外放 / 音箱很近）。本地回声消除压不到零，外放时模型自己的话会被
  转写成一句「用户发言」，它开始回应自己
- 新工具 `connect_mcp_server`：用户说「我装了个 MCP，你连一下」现在真的连得上。先握手连通
  再落盘，失败盘上什么都不留；同名不覆盖，每次调用都要审批
- 聊天附件接受视频、音频与文档：音视频不预先分析，路径随消息交给 agent，由它带着问题去看
  （配了对象存储、模型收得了链接时直接传链接给模型）；文档复用知识库解析。新增 `read_document`；`read_local_file` 按扩展名把二进制挡在读取之前，
  不再让模型硬解出 50KB 乱码
- 进入上下文的图片统一走一道关口：预算内原样通过，超了压缩，压不动换成说明文字 ——
  三种结局都不静默丢弃
- `ue_content_delete` 的 `paths` 直接收目录，展开后一批提交，引擎只跑一遍 GC。此前的
  「逐个删」把模型引到 Python 循环里，282 次 GC 占死编辑器 15 分钟
- `sequence_describe` 新增 `detail="names"`，上限 2000。此前 outline 在 60 条截断，
  截断提示却让人「用 bindings 点名」—— 名字恰好在被截掉的那部分里
- 对象存储（设置 → 对象存储）：聊天里的图片和音视频传进用户自己的 S3 兼容桶，换成链接给模型，
  不再把 base64 跟着对话每轮重发；没配、模型收不了链接或上传失败都安静退回原来的做法。
  生视频（`generate_video`）的参考视频、参考音频也经它换成链接
- 插话能带图、表格、文档和音视频，只拖附件不打字也能插；排着的纯附件消息也能「立即插话」
- 会话可按最后一轮对话重起标题；设置里另有「自动生成新标题」开关，默认关
- 模型与服务商弹窗左侧树加搜索，同时匹配服务商名与模型名；目录弹窗改成分页
- **实验性**：新工具 `ue_autoplay`，自主试玩。`ue_playtest` 碰不到要按键才触发的逻辑，
  它让机器人在 PIE 里自己走、跳、按输入、点菜单。三种模式：探索（找卡住、掉出世界、
  按了就报错的输入）、写死步骤（走到某个 Actor → 按某个输入 → 等某句 PrintString）、
  一句话目标（由判定模型逐步决策，需在 AI 服务里把 TypeSafe Jev 设为「判定」角色）。
  `until_log` 是唯一的断言。目前平地和导航网格走得到的目标比较可靠，要连续跳台阶、爬楼、
  跨沟的路线经常到不了；移动绕开了键位映射和游戏自己的移动蓝图，走得到不等于玩家按键走得到。
  需要更新插件

### 变更

- **对外 MCP 服务默认可写**。老配置里明确存了只读的保持只读；运行中切换权限自动重启服务，
  切到只读时旧的可写会话立即失效
- 主进程请求跟随系统代理（含 PAC 与绕过列表）。此前开着 Clash 的系统代理模式时只有主进程直连，
  国内直连 OpenAI 授权被按地区拒，表现为浏览器里登录成功、回到盒子换令牌失败
- 素材库搜索跨库按相关度统一排序。此前当前库的结果永远排最前，站在 AIGC 库里搜 `horse`，
  另一个库里 3562 个真命中一个都没露面
- 模型与服务商弹窗点保存不再关窗，结果写在测试状态栏里，可以接着测连接
- 插件升到 1.2.9

- **`ue_fixup_redirectors` 的拒绝条件从「无人值守」改成「报告框弹不出来」**：脚本模式、
  没渲染器才拒，框能弹就交给用户点。`-unattended` 仍拒 —— 引擎在那下面直接取消引用者存盘，
  跑完等于什么都没做且不留日志。预演按只读审批，真正执行每次都问，预演上点的
  「本次会话都允许」放不过真正那次
- 运行时信封改印本地时间，带时区偏移与星期几，键名就叫 `now`。此前只有一个 UTC 时间戳藏在
  「连接是这时候查的」括号里，真机上用户凌晨 1:54 问「现在几点」，模型答「14 点 03 分」
- 服务器管理入口暂时隐藏：整页只是 standalone 服务端的客户端，而服务端不在本仓库，
  社区版用户点进去只有一排禁用按钮。页面和路由都留着
- 材质的 `duplicate` / `create_instance` 不再替用户存盘 —— 那会把用户打算丢掉的改动一起
  写进磁盘且撤不回来。改成如实报一句「这次读的是磁盘那份」

### 修复

- 两处编辑器崩溃：`material.add_node` 建完表达式没回写 `Material` 指针，材质编辑器开着时
  建 TextureSample、Constant3Vector 这类节点必崩；`input.map` 对整型属性调浮点取值，
  工程里只要有一个动作挂了 Pulse 或 Combo 触发器就必崩
- 智能标签被子串撞出一堆错标：`SM_Door_01` 会被打上 Diffuse、Material，`SM_Rock_Large`
  被打上 Roughness。规则改为按前缀族 / 后缀族分别匹配，只减少命中不增加。已经写进库里的
  旧标签不会自己消失
- 浏览器窗口被页面自己关掉后 surface 留在表里，之后每次读 URL 都在 Electron 的事件派发里抛
  —— 主进程当场退出
- 用户按停止时返回的是一句原生 `Operation aborted`：没说是谁停的，更没说那条命令可能已经
  发到引擎并且生效了，而模型按「失败」重试会多出第二个 Actor
- 听写会关掉别人正在进行的通话，并继续往一条已经没了的连接里发音频；实时会话的 `stop`
  与 `audio` 两个入口原来不认主人
- 小窗初始消息与侧边上下文不再赌「加载完 500ms」，改由渲染层挂载后主动来取。赌输一次就是
  一个空对话框，日志里一条 error 都没有
- 语音插进来的那句会丢（不进检查点，重启就没了）；自动重起标题会改到用户已经切走的标签页
- 正文以换行开口时，自动朗读把整条答复念两遍，气泡里也重影一段
- Python 里按位置传 `unreal.Rotator` 直接拦下：位置参数是 `(roll, pitch, yaw)`，与 JSON 的
  `(pitch, yaw, roll)` 相反，照搬进 Python 会整批摆错而引擎一个字不报
- `show_ui` 截图改拍用户最后用过的窗口，也可用 `window` 点名。此前开着蓝图编辑器说
  「看看我这张图」，拍回去的是场景
- 工程的引擎版本以磁盘上的 `.uproject` 为准：在 Launcher 里升过版本后，首页卡片不再显示旧版本，
  导入闸门也不再拿旧版本把匹配的资产拦在外面
- 发布时 `latest.yml` 排到最后上传，大文件失败补重试。此前它先传完、安装包 fetch 失败，
  Release 公开挂着新版本却没有可下的包
- 删材质节点少了 `Modify()`，撤销会把已回收的对象放回表达式数组，下次 GC 后重建图表时崩
- 两个选择器掉了颜色、默认应用图标缩水、小窗表头可能一片空白；Agent 模型弹窗在欢迎页顶出窗口
- 小窗运行中三个框各说各话，现在只留一行状态
- `mcp.json` 不是合法 JSON 时，写入会按空配置覆盖掉用户的 server 和密钥；现在拒绝写入。
  连接超时也不再留下没人管的 `npx` 子进程
- 对象存储：R2 控制台给的地址带着桶名，原样粘进来会拼成 `/桶/桶/`；自动清理按上传时间算，
  会把刚在新对话里复用的文件清掉。现在按最后一次用到算
- 52 万行的资产库，不带筛选翻一页要 1.3–2.2 秒，现在不到 1 毫秒；同一秒导入的一批分页时
  不再重复或漏
- 按住说话会中途重启、短句会丢；通话期间小窗不再自动朗读
- 插话在一轮收尾时会回「成功」却丢掉，等附件上传时切了对话会插到别的对话里
- 插件的结构体属性写入在 UE 5.8 上编不过

## [1.0.2] - 2026-09-21

### 新增

- Blender 接入改为一键完成。偏好设置 → MCP 中触发，依赖安装、路径写入、环境变量配置
  由应用执行。未探测到安装位置时可手动指定目录
- `ue_set_transform` 新增 `arrange` 操作：读取每个物体的包围盒，沿指定轴排开，
  `gap` 为边到边间距。`location` 设置的是网格原点，原点相对几何体的位置由资产决定，
  因此按标称尺寸推算的排布对部分资产不成立，且引擎不会报错。`align` 可将一批物体的
  min / max / center 对齐到另一轴的同一条线上。单次上限 100 个
- 蓝图排版算法重写：执行流与数据流共同计算最长路径定列，不再出现反向连线；
  被远处节点引用的纯 getter 就近复制一份，替代跨图长线，`duplicate_getters: false` 可关闭。
  注释框支持读写，整理时随其框选的节点一同移动
- 自动检查到的新版本会在应用内提示。此前 `updater:update-available` 仅由
  「设置 → 关于」面板监听，该面板未挂载时事件丢失。标题栏角标改为三态：
  可更新、下载中、待重启

### 变更

- **破坏性：`sequence_camera_cuts` 不再自动删除已有切轨段。** 此前补全切轨的做法是
  删除全部已有段后重建为一整段，因此一条多机位序列只要存在一帧未覆盖，调用一次即
  只剩一台相机。现在未完全覆盖时直接返回 409 且不做修改，重建需显式传 `rebuild=true`，
  返回中给出删除数量。`sequence_camera_keys` 对应新增 `rebuild_camera_cuts`
- MCP 设置改为逐条读写，保存与删除作用于单条服务。此前的统一「保存并连接」会把
  互不相关的服务绑为一次提交，其中一条无效则全部无法保存
- 工具改为按需加载，低频工具按组折叠，高频工具整组常驻。首轮工具前缀约为
  全量注入的三分之一。只读会话、Ask 模式、以及指定了工具白名单的会话不折叠

### 修复

- 系统代理会导致插件无法连入，且各层检查均为通过状态。虚幻的 WebSocket 实现对所有
  连接套用系统代理，不对 localhost 例外。插件与 `uebox doctor` 现在会输出检测到的
  代理变量；排障文档补充了绕开代理的启动方式及其影响
- 定序器写入纳入事务。此前该命令族未使用 `FUAL_ScopedTransaction`，删除的切轨段与
  覆盖的关键帧无法通过 Ctrl+Z 或 `ue_undo` 撤销，而工具返回成功。同时将全部校验
  前移至修改之前，避免失败时在关卡中残留已生成的相机
- 更新安装失败现在可见。`quitAndInstall()` 原为 void 返回，失败（如用户取消 UAC）
  被静默丢弃，IPC 仍返回成功；下载启动失败在两处调用点均未处理，角标会停留在
  无法响应的「下载中 0%」。未配置更新源时明确提示，不再只显示「正在检查更新」
- `arrange` 中途失败不再返回成功，并给出已完成数量与可用于重试的 `start`
- 两处查询回归：部分索引谓词导致索引失效（52 万行库中打开目录由 0.08 毫秒变为 5.2 秒）；
  FTS 召回截断发生在筛选与分页之前，表现为按目录筛选返回 0 条、翻页超出后为空
- Python 脚本超时后不应继续发送命令探测，工具回执中已说明。脚本运行于编辑器主线程，
  该线程被占用时后续命令（含只读命令）一并超时
- 「刚修改完却提示没有需要保存的内容」对应的原因是该次修改经由 Python 执行，
  未经过事务系统，包未标记为脏，保存被跳过。命中该分支时给出提示
- `adm-zip` 升级至 0.6.1，修复此前未处理的高危公告

### 性能

在 52 万行 / 975 MB 的真实镜像库上，对主进程查询逐条 EXPLAIN 并计时，修复一批
全表扫描与整表载入（修前 → 修后实测）：远端导入写入阶段每文件 4.1 秒 → 0.06 毫秒；
Spotlight 每次按键 5.7 秒 → 走 FTS；筛选下拉 8.3 秒 → 0.3 秒；依赖关系图 50 秒、
堆 1.5 GB → 7 秒；清空回收站的缩略图占用集合 7.1 秒 → 0.4 秒；媒体读取权限
4.7 秒 → 0.03 毫秒。此前项目从不执行 `ANALYZE`，现在打开库时补跑一次。

已知影响与未覆盖项：老库首次打开需新建 6 个索引并执行 ANALYZE，52 万行约 70 秒，
期间界面无响应，仅出现一次，小库为数秒。未处理：FTS 未就绪时的多列 LIKE 兜底查询
（全量同步后约 20 秒内恢复）、网盘备份使用的整表载入、全量同步过程中 52 万行
同时驻留内存（1.5 GB）。

## [1.0.1] - 2026-09-19

应用本身没有变化，装了 1.0.0 的不升级也不影响使用。这一版改的是下载和文档这一侧。

### 修复

- **文档站和官网同域之后，会出现两张首页**。左上角 logo 和 404 的「回首页」原本都指向
  `/`，而 VitePress 是单页应用，它会拦下这次跳转、在前端自己画出文档站首页 ——
  地址栏是 uebox.ai，内容却是手册，刷新一下又变回官网。现在这两处都指向
  `/guide/`，`/` 整个让给官网

### 变更

- **下载入口改指官网下载区**，不再直指 GitHub Releases。历史版本和校验和仍在
  Releases。文档里「只有 GitHub 一个下载源」那段已经不成立，一并删掉

## [1.0.0] - 2026-09-18

首次公开发布。本仓库由闭源产品拆分而来，是**社区核心版**：完全离线运行，
不需要账号，不依赖我们的服务器。下面这些是拆分过程中的用户可见变化。

版本号从这里重新起算 —— 开源之前的内部版本号不对外延续。

### 新增

- **本地直连模型**。在 偏好设置 → 模型 里添加 Provider，把模型绑定给角色即可。
  内置 39 家厂商目录（含图标），涵盖本机推理（Ollama、LM Studio、llama.cpp）、
  自建网关（LiteLLM、One API）、国内外云厂商。目录是随包的静态数据，
  打开它不产生任何网络请求
- **API Key 支持三种写法**，共用一个输入框：直接填密钥（经系统安全存储加密保存）、
  填环境变量名（每次调用现取，不落盘）、或 `!命令`（执行命令取其输出，
  可接 1Password CLI 等）
- **账号登录换密钥**：OpenRouter、ChatGPT、Kimi Code 可以用账号授权，
  不必手工去控制台复制
- **模型角色绑定**：对话 / Agent / 视觉 / 摘要 / 快速 / 嵌入。只配「对话」一项
  就能把整个应用跑起来，其余角色自动回落
- **网页读取改为本地抓取**。把网页存进知识库不再需要任何账号或联网服务；
  只处理静态 HTML，纯前端渲染的页面会明确提示读不到正文
- **工程体检**：可以直接问「分析一下这个项目」「哪些资源最占地方」「这个关卡为什么卡」。
  Agent 会给出全工程的资产占用排行（按磁盘 / 面数 / 贴图内存排，不加载资产所以很快）、
  当前关卡的开销排行（**按资产聚合**，同一个网格摆五百份会告诉你「摆了 500 份、
  总共 194 万面」而不是刷五百行），以及单个资产连同依赖的总体积。
  报告会区分「会坏玩法的」和「费帧的」，缺碰撞排在掉帧前面
- **一段时间的性能采样，不再只是单帧快照**。问「感觉会掉帧」「压力测试一下」，
  Agent 会用引擎自带的 CSV Profiler 连续采样一段时间，给出 p50/p95/p99 帧时间、
  最卡的那一帧详情、以及瓶颈在 GPU 还是哪个线程——而不是被平均值抹平的一个数字。
  需要更细粒度（具体哪个函数、哪个渲染 Pass）时，可以再录一段 Unreal Insights trace，
  Agent 会自动定位本机对应版本的 UnrealInsights.exe、无头跑分析、把耗时排行整理出来
- **知识库 Studio 的六类产出改在本地生成**：思维导图、研究报告、知识图谱、
  模拟面试、网页、头脑风暴。用的是你自己在 偏好设置 → 模型 里绑的模型。
  此前这六个按钮点下去都会弹一条内部错误：「当前版本没有官方服务端会话，
  已拦截请求：/api/ai/v1/notebook/task」—— 它们原本是提交给官方服务端的异步任务。
  现在没有排队、没有任务队列、没有计费；生成过程中进度条跟着模型实际吐出的字数走，
  点「取消」会真的掐断请求，而不只是让界面不再等
- 一条命令的验收门禁 `pnpm verify`，以及离线启动门禁：在打包产物上逐个功能页
  验证不产生任何指向官方服务器的请求
- **几何编辑**：可以直接说「把这两个模型合并」「在这面墙上开个洞」「面数太高帮我减一下」
  「这模型有破面补一下」。走引擎自带的 Geometry Script，覆盖布尔运算、切割、减面、
  重网格、补洞、展 UV、生成碰撞，改完能读回面数和封闭性做校验。
  需要先在工程里启用 GeometryScripting 插件并重启编辑器（Agent 会提示）。
  建模模式里的雕刻、PolyEdit 这类交互式工具引擎没给脚本接口，够不着

### 变更

- **蓝图库 / 材质库里的 AI 助手换成了应用里那个真助手**。此前那个面板是独立的
  一问一答聊天框：没有工具、看不见你的引擎、不会用技能，只能对着库里存的
  那段文本猜。现在嵌的是主助手本身（和知识库详情页同一个），整套引擎工具、
  审批门、技能、语音都在。它还知道你正在看哪个条目、在画布上选中了哪几个节点。
  **破坏性**：旧面板的历史问答记录不迁移，那是问答草稿不是你的创作 ——
  蓝图和材质数据本身一个字都没动
- **蓝图片段改用引擎自己的复制粘贴格式存取**。此前是把节点拆成结构化描述再照着
  重建，用户手动改过形状的节点（比如加过第三个输出的 Sequence）搬过去会少东西，
  所以那类片段一律拒绝保存。现在走的是编辑器 Ctrl+C / Ctrl+V 那条路，
  **编辑器里复制粘贴保得住的都保得住**，能存的范围宽了一大截。
  需要工程里的 UnrealAgentLink 插件是新版（太旧会在写入前拦下并提示更新）
- **编辑器里的 Python 不再假失败**。此前跑超过 30 秒的脚本一律被报成失败 ——
  哪怕它其实跑成功了。现在改走插件的带返回值通道，`print()` 的内容和 Python
  异常栈都会原样回来，上限提到 5 分钟。定序器工具和「执行 Python 脚本」都受益
- **破坏性**：不再有账号体系。启动直接进主界面，没有登录、没有区域门禁、
  没有订阅与配额
- **破坏性**：知识库检索需要自行配置**嵌入模型**（此前由官方服务
  提供）。想完全本地免费，装 Ollama 后拉 `nomic-embed-text` 或 `bge-m3`。
  更换嵌入模型会自动重建向量索引 —— 维度与语义空间都变了，旧向量无法复用
- **不再随包分发 FFmpeg**（安装包减少约 80MB）。录屏导出 mp4/gif、音频提取、
  视频压缩改为使用系统里安装的 FFmpeg，未安装时会提示安装方式
  （Windows `winget install Gyan.FFmpeg`、macOS `brew install ffmpeg`）；
  装好后无需重启应用。**视频缩略图不受影响** —— 已改用 Chromium 自带解码

### 移除

- **破坏性**：卸载「工作区（Workspace）」
- **破坏性**：卸载「工作流 Nexus」
- **破坏性**：卸载 AIGC 视频生成、视频审阅与音乐生成（图片与 3D 生成保留）
- **蓝图库不再有云同步入口**。此前是一个灰掉的云图标，提示「请先登录」——
  而社区版根本没有登录这回事。现在整个按钮与同步徽标都不渲染。
  蓝图数据本身完全不受影响：新建、编辑、删除、集合、从旧版导入都是本地能力
- **知识库不再有分享与「按分享码导入」入口**。分享按钮此前在社区版是**可点的**
  （它判的是本机身份，不是官方会话），点开就会往官方服务端发请求。
  现在分享按钮、导入按钮与相关弹层整块不渲染，`?importToken=` 深链也一并移除。
  知识库本身完全不受影响：新建、来源管理、RAG 索引、Studio 产出都是本地能力
- **无限画布不再有云端画布与协作**。列表页只显示本地草稿，「收藏」「共享」两个
  纯云端筛选、卡片上的分享菜单、编辑器里的「转为云端画布并分享」都不渲染，
  云端画布与分享链接两条路由也不注册。画布本身完全不受影响：新建、编辑、
  本地草稿自动保存、媒体导入、导出都是本地能力。
  此前社区版第一次打开画布页会弹一条内部错误：「当前版本没有官方服务端会话，
  已拦截请求：/api/boards?...」
- **破坏性：移除应用内虚拟币与快速额度体系**。钱包、额度、价格表和购买触发均已删除；
  Jina 网页搜索改为用户 Key 直连（未配置时走免密钥搜索），深度研究只使用用户自己的
  Jina Key，不再经过平台代理或平台扣费
- **知识库生成的网页不再有「分享」按钮**。分享是把 HTML 传到官方服务端换一个公开
  链接，社区版没有那台服务器，按钮点下去必然报错。网页本身完全不受影响：
  生成、预览、下载、复制都是本地能力。页面右下角的「Powered by UEBOX.AI」
  角标也一并去掉了 —— 它是为公开分享页准备的，而现在网页只在你自己机器上
- 移除依赖 `jschardet`（无任何调用方，且为 LGPL 许可）

### 安全

- **移除全部遥测**。社区版不再包含 Sentry 的任何代码与依赖
- 漏洞报告流程见 [SECURITY.md](SECURITY.md)
- API Key 一律经系统安全存储加密后单独保存，不写进 `models.json`，
  也不回读到界面。安全存储不可用时**拒绝落盘**，而不是降级成明文
- 第三方组件许可证与分发说明见 [THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md)
